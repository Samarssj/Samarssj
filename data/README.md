# Training data

Place the public `creditcard.csv` benchmark file in this directory before running the training script:

```text
data/creditcard.csv
```

The raw file is intentionally excluded from version control because it is approximately 144 MB. Public sources and attribution details are documented in the project README.
